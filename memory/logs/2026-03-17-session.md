# 2026-03-17

## AutoPax Trucking Leads & Filtering Project
- Built a multi-threaded Python script (`tag_nationality_historical.py`) that uses the Gemini API (`gemini-3.1-flash-lite-preview`) to backfill classification of 80,000+ trucking leads by owner nationality (Ethiopian, Eritrean, Indian, Pakistani) based on company names and emails.
- Achieved processing speeds of ~1,000 leads per round trip by using `ThreadPoolExecutor` with 10 concurrent workers. Handled 429 / 504 API rate limit errors gracefully with exponential backoff.
- Tried Anthropic (`claude-3-haiku-20240307`) via an `ORDER BY created_at DESC` bottom-up script to parallelize with Gemini, but hit immediate rate-limits. Deprecated Anthropic entirely in favor of Gemini for this task.
- Wired the new `tags` database column into the AutoPax Trucking CRM (Next.js/React frontend):
  - Added a "Nationality" dropdown filter next to the state filter.
  - Filter operates directly against the Supabase backend via `.contains('tags', [nationalityFilter])`.
  - Added visual pills (badges) under company names to highlight tagged nationalities.
  - Pushed the change live and confirmed it filters correctly on the frontend.
  - Patched the "Export CSV" feature so that exporting filtered records also includes the new Nationality column in the exported file.
- Modified the daily 3:00 AM FMCSA data sync (`sync_daily_optimized.py`) per Robel's request to completely skip downloading "Pending Cancellations." The pipeline now exclusively pulls New Ventures and 90-Day Renewals to ensure maximum data relevance.
