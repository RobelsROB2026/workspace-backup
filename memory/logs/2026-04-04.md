
## 2026-04-04 System Updates & Routing Changes

### LLM Knowledge Base Architecture (KB Ingest Protocol)
- Re-architected `~/research/` library into localized knowledge bases (wikis) based on Karpathy's methodology, using Claude Code as the compiler.
- Each topic (trucking, social-auto, bonds, hypnosis, etc.) now has a `raw/` folder for document ingest.
- Created `wiki_compile.py` to incrementally synthesize raw data into localized `KNOWLEDGE.md` files.
- Created `wiki_search.py` for CLI-based Q&A. Subagents are physically restricted to search only within their topic boundaries (`--topic <name>`), while the main agent has global search access.
- Created `wiki_lint.py` to health-check the wiki and maintain data integrity.
- Adopted the KB Ingest Protocol in `SOUL.md`: all new learnings are dumped into `raw/` and compiled instead of hoarded in chat logs.

### Agent Routing & Rigid Isolation Enforced
- Corrected and strictly enforced channel routing rules in `openclaw.json` to establish rigid sub-agent boundaries. Agents cannot access each other's workspaces or contexts.
- **Topic 2:** Bonds project
- **Topic 3:** New York Tour Bus
- **Topic 96:** FMCSA (Trucking Leads & Nationality Tagger)
- **Topic 419:** Social Media Manager
- **Topic 943:** Hypnosis & NLP
- **Topic 1051:** Gemma Agent (New isolated agent running local `ollama/gemma4:e4b` model)
- **Main Agent (ROBA):** DMs (Global orchestration)

### Background Jobs & Infrastructure
- Cleaned up legacy macOS `launchd` `.plist` files; OpenClaw native cron is now fully handling scheduling.
- Properly assigned cron jobs to their specific isolated agents: FMCSA/Tagger to the `trucking` agent, NLP ingest to the `hypnosis` agent.
- Updated the Hypnosis `engine.sh` callback to correctly send daily reports directly to Topic 943 instead of the main DM.
